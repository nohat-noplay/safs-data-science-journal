Learns from training data too well, capturing patterns in the noise and outliers.
Model will have high training accuracy and low test accuracy and will be complex. 
**Fixes**: simplify model, regularization, cross validation, early stopping (eg. Decision Trees)